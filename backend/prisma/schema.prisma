// GraphSafe backend foundation — Prisma schema.
// No application models yet. This file exists to verify database
// connectivity ahead of future features (auth, projects, dependency graph).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
