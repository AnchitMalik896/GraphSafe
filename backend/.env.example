# Server
PORT=4000
NODE_ENV=development

# Database (Neon-compatible PostgreSQL connection string)
DATABASE_URL="postgresql://user:password@host:5432/graphsafe?schema=public"

# Auth
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN="http://localhost:3000"
